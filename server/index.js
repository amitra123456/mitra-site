import express from "express"`nimport cors from "cors"`nimport path from "path"`nimport { fileURLToPath } from "url"`nimport contentRoutes from "./routes/content.js"`nimport authRoutes from "./routes/auth.js"`n`nconst __filename = fileURLToPath(import.meta.url)`nconst __dirname = path.dirname(__filename)`nconst app = express()`nconst port = process.env.PORT || 3001`nconst allowedOrigin = process.env.VITE_API_BASE || "http://localhost:5173"`n`napp.use(cors({ origin: allowedOrigin, credentials: true }))`napp.use(express.json())`napp.use("/content", express.static(path.resolve(__dirname, "../content")))`napp.use("/api/content", contentRoutes)`napp.use("/api/auth", authRoutes)`napp.get("/", (req, res) => res.send({ status: "ok" }))`n`napp.listen(port, () => {`n  console.log(`Server listening on http://localhost:${port}`)`n})`n
