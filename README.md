# Mitra Site`n`nA clean React + Vite frontend and Express backend scaffold for a personal branding site.`n`n## Local Development`n`n1. Copy `.env.example` to `.env` and customize if needed.`n2. Run `npm run install:all` from the repository root.`n3. Start both servers with `npm run dev`.`n`nFrontend: http://localhost:5173`nBackend API: http://localhost:3001`n`n## Project Structure`n`n- `client/` — React app with Tailwind CSS and React Router`n- `server/` — Express API with content CRUD and JWT auth`n- `content/` — JSON content store for public content sections`n
