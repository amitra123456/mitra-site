{
  "name": "mitra-site",
  "version": "1.0.0",
  "description": "Aniruddha Mitra — Personal Brand Site & The AI Leaders Circle",
  "scripts": {
    "build": "node scripts/build-manifest.js",
    "dev": "npx serve public -p 3000",
    "predeploy": "npm run build",
    "deploy": "git add . && git commit -m 'deploy: update content' && git push origin main",
    "new-research": "node scripts/new-content.js research",
    "new-blog": "node scripts/new-content.js blog",
    "new-portfolio": "node scripts/new-content.js portfolio",
    "new-speaking": "node scripts/new-content.js speaking"
  },
  "devDependencies": {
    "serve": "^14.2.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
