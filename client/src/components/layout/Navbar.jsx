import { NavLink } from "react-router-dom"`n`nconst links = [`n  { href: "/", label: "Home" },`n  { href: "/admin/login", label: "Admin" }`n]`n`nexport default function Navbar() {`n  return (`n    <header className="border-b border-slate-200 bg-white/90 backdrop-blur">`n      <div className="mx-auto flex max-w-6xl items-center justify-between px-6 py-4">`n        <a href="/" className="text-xl font-semibold text-navy">Mitra</a>`n        <nav className="flex gap-4">`n          {links.map((link) => (`n            <NavLink`n              key={link.href}`n              to={link.href}`n              className={({ isActive }) => `text-sm font-medium transition ${isActive ? "text-navy" : "text-slate-600 hover:text-navy"}`}`n            >`n              {link.label}`n            </NavLink>`n          ))}`n        </nav>`n      </div>`n    </header>`n  )`n}`n
