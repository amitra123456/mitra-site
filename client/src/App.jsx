import { Routes, Route, Navigate } from "react-router-dom"`nimport Navbar from "./components/layout/Navbar"`nimport Footer from "./components/layout/Footer"`nimport Home from "./routes/Home"`nimport Login from "./routes/admin/Login"`nimport Dashboard from "./routes/admin/Dashboard"`n`nexport default function App() {`n  return (`n    <div className="min-h-screen bg-cream text-slate-900">`n      <Navbar />`n      <main className="mx-auto max-w-6xl px-6 py-10">`n        <Routes>`n          <Route path="/" element={<Home />} />`n          <Route path="/admin/login" element={<Login />} />`n          <Route path="/admin/dashboard" element={<Dashboard />} />`n          <Route path="*" element={<Navigate to="/" replace />} />`n        </Routes>`n      </main>`n      <Footer />`n    </div>`n  )`n}`n
