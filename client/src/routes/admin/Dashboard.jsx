import { useEffect, useState } from "react"`nimport { useNavigate } from "react-router-dom"`nimport { api } from "../../lib/api"`n`nconst sections = [`n  { value: "research", label: "Research" },`n  { value: "insights", label: "Insights" },`n  { value: "work", label: "Work" }`n]`n`nexport default function Dashboard() {`n  const [section, setSection] = useState("research")`n  const [title, setTitle] = useState("")`n  const [description, setDescription] = useState("")`n  const [status, setStatus] = useState("")`n  const navigate = useNavigate()`n  const token = localStorage.getItem("mitra_auth_token")`n`n  useEffect(() => {`n    if (!token) navigate("/admin/login")`n  }, [navigate, token])`n`n  async function handleSubmit(event) {`n    event.preventDefault()`n    setStatus("Saving...")`n`n    try {`n      await api.post(`/api/content/${section}`, { title, description }, {`n        headers: { Authorization: `Bearer ${token}` }`n      })`n      setStatus("Content saved successfully.")`n      setTitle("")`n      setDescription("")`n    } catch {`n      setStatus("Unable to save content. Please try again.")`n    }`n  }`n`n  return (`n    <div className="mx-auto max-w-3xl rounded-3xl border border-slate-200 bg-white p-10 shadow-sm">`n      <div className="flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">`n        <div>`n          <h2 className="text-3xl font-semibold text-navy">Admin Dashboard</h2>`n          <p className="mt-2 text-slate-600">Add new items to the public content library.</p>`n        </div>`n      </div>`n      <form onSubmit={handleSubmit} className="mt-8 space-y-6">`n        <label className="block">`n          <span className="text-sm font-medium text-slate-700">Section</span>`n          <select`n            value={section}`n            onChange={(e) => setSection(e.target.value)}`n            className="mt-2 w-full rounded-2xl border border-slate-300 bg-slate-50 px-4 py-3 text-slate-900 focus:border-navy focus:outline-none focus:ring-2 focus:ring-navy/10"`n          >`n            {sections.map((item) => (`n              <option key={item.value} value={item.value}>{item.label}</option>`n            ))}`n          </select>`n        </label>`n        <label className="block">`n          <span className="text-sm font-medium text-slate-700">Title</span>`n          <input`n            value={title}`n            onChange={(e) => setTitle(e.target.value)}`n            className="mt-2 w-full rounded-2xl border border-slate-300 bg-slate-50 px-4 py-3 text-slate-900 focus:border-navy focus:outline-none focus:ring-2 focus:ring-navy/10"`n            placeholder="Enter content title"`n          />`n        </label>`n        <label className="block">`n          <span className="text-sm font-medium text-slate-700">Description</span>`n          <textarea`n            value={description}`n            onChange={(e) => setDescription(e.target.value)}`n            rows="4"`n            className="mt-2 w-full rounded-2xl border border-slate-300 bg-slate-50 px-4 py-3 text-slate-900 focus:border-navy focus:outline-none focus:ring-2 focus:ring-navy/10"`n            placeholder="Enter a short summary"`n          />`n        </label>`n        {status && <p className="text-sm text-slate-700">{status}</p>}`n        <button`n          type="submit"`n          className="inline-flex items-center justify-center rounded-full bg-navy px-6 py-3 text-sm font-semibold text-white transition hover:bg-slate-900"`n        >`n          Save content`n        </button>`n      </form>`n    </div>`n  )`n}`n
