import { useState } from "react"`nimport { useNavigate } from "react-router-dom"`nimport { api } from "../../lib/api"`n`nexport default function Login() {`n  const [password, setPassword] = useState("")`n  const [error, setError] = useState("")`n  const [loading, setLoading] = useState(false)`n  const navigate = useNavigate()`n`n  async function handleSubmit(event) {`n    event.preventDefault()`n    setLoading(true)`n    setError("")`n`n    try {`n      const response = await api.post("/api/auth/login", { password })`n      localStorage.setItem("mitra_auth_token", response.data.token)`n      navigate("/admin/dashboard")`n    } catch {`n      setError("Invalid password. Please try again.")`n    } finally {`n      setLoading(false)`n    }`n  }`n`n  return (`n    <div className="mx-auto max-w-2xl rounded-3xl border border-slate-200 bg-white p-10 shadow-sm">`n      <h2 className="text-3xl font-semibold text-navy">Admin Login</h2>`n      <p className="mt-3 text-slate-600">Enter the admin password to manage public content.</p>`n      <form onSubmit={handleSubmit} className="mt-8 space-y-6">`n        <label className="block">`n          <span className="text-sm font-medium text-slate-700">Password</span>`n          <input`n            type="password"`n            value={password}`n            onChange={(e) => setPassword(e.target.value)}`n            className="mt-2 w-full rounded-2xl border border-slate-300 bg-slate-50 px-4 py-3 text-slate-900 focus:border-navy focus:outline-none focus:ring-2 focus:ring-navy/10"`n            placeholder="Enter admin password"`n          />`n        </label>`n        {error && <p className="text-sm text-red-600">{error}</p>}`n        <button`n          type="submit"`n          disabled={loading}`n          className="inline-flex items-center justify-center rounded-full bg-navy px-6 py-3 text-sm font-semibold text-white transition hover:bg-slate-900 disabled:cursor-not-allowed disabled:opacity-70"`n        >`n          {loading ? "Signing in…" : "Sign in"}`n        </button>`n      </form>`n    </div>`n  )`n}`n
